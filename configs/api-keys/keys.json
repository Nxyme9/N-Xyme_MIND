{
  "opencode": [
    {
      "key": "${OPENCODE_API_KEY}",
      "rpm_limit": 60,
      "tpm_limit": 100000,
      "daily_limit": 200,
      "provider": "opencode"
    }
  ],
  "openrouter": [
    {
      "key": "${OPENROUTER_API_KEY}",
      "rpm_limit": 60,
      "tpm_limit": 100000,
      "daily_limit": 50,
      "provider": "openrouter"
    }
  ],
  "google": [
    {
      "key": "${GOOGLE_API_KEY}",
      "rpm_limit": 60,
      "tpm_limit": 100000,
      "provider": "google"
    }
  ],
  "groq": [
    {
      "key": "${GROQ_API_KEY}",
      "rpm_limit": 30,
      "tpm_limit": 6000,
      "provider": "groq"
    }
  ]
}
