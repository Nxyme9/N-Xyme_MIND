---
description: "Manually advance a loop by one iteration — increments counter in state"
---

Manually force-advance a running loop by one iteration.

Usage:
  ralph_iterate(session_id="...")

Note: In normal operation the plugin auto-advances on each message.updated.
This tool is for manual control or testing.
