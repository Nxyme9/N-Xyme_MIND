---
description: "Cancel an active loop — stops it permanently and clears state"
---

Cancel a running Ralph or Ultrawork loop. This deletes the state file permanently.

Usage:
  ralph_cancel(session_id="...")

Also available as: ulw_cancel(session_id="...")
