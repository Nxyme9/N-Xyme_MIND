---
description: "Cancel an active Ultrawork loop — stops and clears state"
---

Cancel a running Ultrawork loop. This deletes the state file permanently.

Usage:
  ulw_cancel(session_id="...")

Alias for: ralph_cancel(session_id="...")
