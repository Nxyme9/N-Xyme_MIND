# code_search
**Purpose:** Find code by pattern — used to check if known bad patterns exist elsewhere.
**Usage:** `code_search("unsafe pointer usage")`
**Optimized for:** Critic — finds SIMILAR CODE to known buggy patterns.
