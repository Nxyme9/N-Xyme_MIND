# memory_search
**Purpose:** Search holographic memory — focused on PAST BUGS, DECISIONS, and FAILURES.
**Usage:** `memory_search("past bug: memory leak in daemon")`
**Optimized for:** Critic — finds PAST MISTAKES to check if they're repeated.
