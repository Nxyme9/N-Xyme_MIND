# dependency_finder
**Purpose:** Find what depends on a file and what a file depends on.
**Usage:** `dependency_finder("src/daemon.mojo")` → shows import chains
**Optimized for:** Search — maps code RELATIONSHIPS.
