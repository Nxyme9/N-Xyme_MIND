# Unified AGENTS.md Overhaul - ADHD-Friendly + Schema-Safe + Lesson-Proof

## TL;DR

> **Quick Summary**: Overhaul AGENTS.md across MIND and CATALYST with unified rules synthesized from 20+ agents, 3 codebases, 10 deprecated iterations, and cutting-edge 2025-2026 LLM orchestration research. Add ADHD protocols, schema safety, compression guard, and lessons from every previous failure.
>
> **Deliverables**:
> - Updated `/home/nxyme/N-Xyme_MIND/AGENTS.md` with unified rules
> - Updated `/run/media/nxyme/NXYME_CORE/01_CODING/00_N-Xyme_CATALYST/AGENTS.md` with unified rules
> - New `.sisyphus/rules/global-rules.md` with cross-project rules
>
> **Estimated Effort**: Medium
> **Parallel Execution**: YES - 3 waves
> **Critical Path**: Task 1 → Task 2 → Task 3 → Task 7 → Task 8 → Task 9

---

## Context

### Original Request
"Review all agent.md instructions we had in /home/nxyme/N-Xyme_MIND/ and the cutting edge research in docs for llm orchestration and add the best rules here plus a few more if we don't have, agents should always use ADHD friendly frictionless modular preferences, and check schema when needed to avoid 10 hour rewriting comas. Do a 12 agent deepdive delegation into MIND!"

Plus: "Same for /run/media/nxyme/NXYME_CORE/01_CODING/00_N-Xyme_CATALYST/. Learn from all my previous work attempts and mistakes!"

Plus: "Check /run/media/nxyme/NXYME_CORE/99_Depricated/ - same treatment!"

**CRITICAL USER MANDATE**: "dont touch compression we went through hell before!!!!!!!!!!!!!!!!!!!!!!!!"

### Interview Summary

**20+ agents fired across 4 domains:**
- 6 agents: MIND AGENTS.md analysis, structure, ADHD research, schema research, LLM orchestration, agent communication
- 5 agents: CATALYST AGENTS.md analysis, structure, configs, memory/handoff, code patterns
- Direct reads: 10 AGENTS.md files from deprecated directory
- 1 agent: Deprecated config evolution (still running at plan generation time)
- 1 Metis consultation for gap analysis

**Key Discussions**:
- Compression: ABSOLUTE DO NOT TOUCH. User went through 12+ hours of hell. Hooks are DISABLED for good reason.
- ADHD: Current rules are "acknowledged but not systematized"
- Schema: CATALYST has schema checking rule, MIND doesn't — CRITICAL gap
- Models: Omni models caused loops in evaluation agents (deprecated lesson)
- Scope creep: 8 previous iterations failed from feature bloat (deprecated lesson)

### Research Findings

**From 2025-2026 LLM Orchestration:**
- LangGraph state machines are gold standard for agent flows
- Four-phase context lifecycle (Prevention → Conservation → Compaction → Recovery)
- Most-specific-wins for AGENTS.md hierarchy conflicts
- Versioned schemas for backward compatibility

**From ADHD Research:**
- Chunked output (max 5 items per block)
- Step indicators ("Step X of Y")
- Decision fatigue prevention (binary choices max)
- Context anchors ("We are working on [X]")
- Progressive disclosure hooks

**From Schema Research:**
- Pre-flight validation (LSP before code)
- Contract-first read (interfaces before implementation)
- Incremental checking (after each file)
- Boundary scope lock (only touch in-scope files)

**From Deprecated Directory (10 AGENTS.md files):**
- 7 recurring mistakes identified
- 8 successful patterns survived all iterations
- Compression/truncation caused catastrophic failures
- Omni models caused infinite loops
- Scope creep killed 6/8 iterations

### Metis Review
**Identified Gaps** (addressed):
- No version field in AGENTS.md — will add
- No rollback strategy — will document
- Compression rules ambiguous — will clarify "DO NOT TOUCH"
- Config file ownership unclear — will define source of truth
- Agent-specific vs global rules — will add context-activated blocks

---

## Work Objectives

### Core Objective
Create a unified, ADHD-friendly, schema-safe, lesson-proof AGENTS.md system across MIND and CATALYST that combines the best rules from all three sources (MIND, CATALYST, deprecated) plus cutting-edge research.

### Concrete Deliverables
1. Updated `/home/nxyme/N-Xyme_MIND/AGENTS.md` (~400 lines max)
2. Updated `/run/media/nxyme/NXYME_CORE/01_CODING/00_N-Xyme_CATALYST/AGENTS.md` (~400 lines max)
3. New `.sisyphus/rules/global-rules.md` with cross-project rules

### Definition of Done
- [ ] MIND AGENTS.md ≤ 500 lines
- [ ] CATALYST AGENTS.md ≤ 500 lines
- [ ] All existing working rules preserved verbatim
- [ ] Each new section has concrete example
- [ ] No references to non-existent files
- [ ] Compression section explicitly says "DO NOT TOUCH"
- [ ] Schema checking protocol present in both files
- [ ] ADHD protocols systematized
- [ ] Lessons from deprecated documented

### Must Have
- ADHD Operating Protocol (chunked output, step indicators, context anchors)
- Schema Safety Protocol (pre-flight LSP, contract-first, incremental checking)
- Compression Guard (explicit DO NOT TOUCH with history of why)
- Circuit Breakers from MIND preserved
- Schema checking rule from CATALYST adopted
- No agent duplication rule from CATALYST adopted
- Omni model lesson from deprecated documented
- Scope creep lesson from deprecated documented

### Must NOT Have (Guardrails)
- NO compression hook changes (DISABLED hooks stay DISABLED)
- NO re-enabling context-window-monitor
- NO re-enabling preemptive-compaction
- NO re-enabling anthropic-context-window-limit-recovery
- NO Docker rules (user constraint: native Linux)
- NO hardcoded limits (use dynamic/configurable)
- NO references to files that don't exist
- NO rules requiring OpenCode plugin support not yet available
- NO modification of MCP server configurations
- NO modification of model assignments without user approval

---

## Verification Strategy

### Test Decision
- **Infrastructure exists**: N/A (documentation only)
- **Automated tests**: None (documentation only)
- **Verification**: Line count checks, grep checks, manual review

### QA Policy
Each task includes agent-executable QA scenarios using Bash (grep, wc) and manual review.

---

## Execution Strategy

### Parallel Execution Waves

```
Wave 1 (Start Immediately — foundation):
├── Task 1: Unified rules extraction from draft [quick]
├── Task 2: MIND AGENTS.md rewrite [deep]
└── Task 3: CATALYST AGENTS.md rewrite [deep]

Wave 2 (After Wave 1 — new sections):
├── Task 4: ADHD Operating Protocol section [quick]
├── Task 5: Schema Safety Protocol section [quick]
├── Task 6: Compression Guard section [quick]
├── Task 7: Loop Prevention + Circuit Breakers section [quick]
└── Task 8: Deprecated Lessons section [quick]

Wave 3 (After Wave 2 — integration + verification):
├── Task 9: Global rules file (.sisyphus/rules/global-rules.md) [quick]
├── Task 10: Final verification + cleanup [quick]
└── Task 11: Delete draft file [quick]
```

### Dependency Matrix
- **1**: — → 2, 3
- **2**: 1 → 4, 5, 6, 7, 8
- **3**: 1 → 4, 5, 6, 7, 8
- **4-8**: 2, 3 → 9
- **9**: 4-8 → 10
- **10**: 9 → 11

### Agent Dispatch Summary
- **Wave 1**: 3 tasks — T1 → `quick`, T2 → `deep`, T3 → `deep`
- **Wave 2**: 5 tasks — T4-T8 → `quick`
- **Wave 3**: 3 tasks — T9-T11 → `quick`

---

## TODOs

- [ ] 1. Extract unified rules from draft synthesis

  **What to do**:
  - Read `.sisyphus/drafts/agents-md-overhaul.md` to extract the finalized rules
  - Create a "unified rules inventory" listing every rule to be included, with source attribution (MIND/CATALYST/Deprecated/Research)
  - Categorize rules: ADHD, Schema, Compression, Delegation, Memory, Error Handling, Loop Prevention, Progressive Disclosure, Context-Activated
  - Mark each rule as: ADOPT (keep as-is), MERGE (combine sources), NEW (from research), GUARD (protect from changes)

  **Must NOT do**:
  - Do NOT modify any AGENTS.md files yet (this is extraction only)
  - Do NOT touch compression hooks
  - Do NOT delete the draft file

  **Recommended Agent Profile**:
  - **Category**: `quick`
    - Reason: Reading and categorizing existing research, no code changes
  - **Skills**: []
    - No special skills needed for document extraction

  **Parallelization**:
  - **Can Run In Parallel**: YES
  - **Parallel Group**: Wave 1 (with Tasks 2, 3)
  - **Blocks**: Tasks 4-8
  - **Blocked By**: None (can start immediately)

  **References**:
  - `.sisyphus/drafts/agents-md-overhaul.md` — Complete synthesis from 21+ agents
  - `/home/nxyme/N-Xyme_MIND/AGENTS.md` — Current MIND rules (247 lines)
  - `/run/media/nxyme/NXYME_CORE/01_CODING/00_N-Xyme_CATALYST/AGENTS.md` — Current CATALYST rules (239 lines)

  **Acceptance Criteria**:
  - [ ] Unified rules inventory created with ≥ 30 rules categorized
  - [ ] Each rule has source attribution (MIND/CATALYST/Deprecated/Research)
  - [ ] Each rule has action tag (ADOPT/MERGE/NEW/GUARD)
  - [ ] Compression rules marked as GUARD with "DO NOT TOUCH" note

  **QA Scenarios**:

  ```
  Scenario: Rules inventory completeness
    Tool: Bash (grep)
    Preconditions: Draft file exists at .sisyphus/drafts/agents-md-overhaul.md
    Steps:
      1. Read the draft file
      2. Count distinct rules extracted
      3. Verify all 9 categories present (ADHD, Schema, Compression, Delegation, Memory, Error Handling, Loop Prevention, Progressive Disclosure, Context-Activated)
    Expected Result: ≥ 30 rules across all 9 categories
    Failure Indicators: Missing categories, fewer than 30 rules
    Evidence: .sisyphus/evidence/task-1-rules-inventory.txt

  Scenario: Compression guard present
    Tool: Bash (grep)
    Preconditions: Rules inventory exists
    Steps:
      1. grep "DO NOT TOUCH" in rules inventory
      2. grep "GUARD" tag for compression rules
    Expected Result: Compression rules tagged as GUARD with DO NOT TOUCH
    Failure Indicators: No GUARD tag, no DO NOT TOUCH mention
    Evidence: .sisyphus/evidence/task-1-compression-guard.txt
  ```

  **Commit**: NO (grouped with final)

---

- [ ] 2. Rewrite MIND AGENTS.md with unified rules

  **What to do**:
  - Read current `/home/nxyme/N-Xyme_MIND/AGENTS.md` (247 lines)
  - Rewrite using unified rules inventory from Task 1
  - PRESERVE VERBATIM: "Never Do" section, VPN section, User Preferences section, Memory System section, Circuit Breakers section
  - ADD NEW: ADHD Operating Protocol, Schema Safety Protocol, Compression Guard, Loop Prevention, Context-Activated Rules
  - KEEP ≤ 500 lines total
  - Use markdown headers for clear sections
  - Add version field: `<!-- Version: 3.0 | Updated: 2026-03-31 -->`

  **Must NOT do**:
  - Do NOT remove existing working rules
  - Do NOT change compression hooks or behavior
  - Do NOT modify MCP server configurations
  - Do NOT change model assignments
  - Do NOT reference files that don't exist

  **Recommended Agent Profile**:
  - **Category**: `deep`
    - Reason: Complex document rewrite requiring understanding of all research sources
  - **Skills**: []
    - No special skills needed

  **Parallelization**:
  - **Can Run In Parallel**: YES
  - **Parallel Group**: Wave 1 (with Tasks 1, 3)
  - **Blocks**: Tasks 4-8
  - **Blocked By**: Task 1 (needs rules inventory)

  **References**:
  - `/home/nxyme/N-Xyme_MIND/AGENTS.md` — Current file to preserve + extend
  - `.sisyphus/drafts/agents-md-overhaul.md` — Research synthesis
  - CATALYST schema checking rule: "ALWAYS CHECK SCHEMA BEFORE MODIFYING CONFIGS"
  - CATALYST no-duplication rule: "NEVER DUPLICATE AGENTS BETWEEN CONFIGS"
  - Deprecated lesson: "Metis/Momus were omni — caused loops" → use fast models for evaluation
  - Deprecated lesson: "aggressive_truncation: false" → never truncate context

  **Acceptance Criteria**:
  - [ ] File ≤ 500 lines (`wc -l` check)
  - [ ] Version header present
  - [ ] "Never Do" section preserved verbatim
  - [ ] VPN section preserved verbatim
  - [ ] Circuit Breakers section preserved
  - [ ] New "ADHD Operating Protocol" section present
  - [ ] New "Schema Safety Protocol" section present
  - [ ] New "Compression Guard" section present with "DO NOT TOUCH"
  - [ ] No references to non-existent files

  **QA Scenarios**:

  ```
  Scenario: Line count within budget
    Tool: Bash (wc -l)
    Preconditions: Rewritten AGENTS.md exists
    Steps:
      1. Run: wc -l /home/nxyme/N-Xyme_MIND/AGENTS.md
      2. Verify output ≤ 500
    Expected Result: Line count ≤ 500
    Failure Indicators: Line count > 500
    Evidence: .sisyphus/evidence/task-2-line-count.txt

  Scenario: Existing rules preserved
    Tool: Bash (grep)
    Preconditions: Rewritten AGENTS.md exists
    Steps:
      1. grep -c "Never Do" → ≥ 1
      2. grep -c "VPN" → ≥ 1
      3. grep -c "Circuit Breaker" → ≥ 1
      4. grep -c "graphiti" → ≥ 1
    Expected Result: All existing sections present
    Failure Indicators: Any grep returns 0
    Evidence: .sisyphus/evidence/task-2-preservation.txt

  Scenario: New sections present
    Tool: Bash (grep)
    Preconditions: Rewritten AGENTS.md exists
    Steps:
      1. grep -c "ADHD" → ≥ 2
      2. grep -c "Schema Safety" → ≥ 1
      3. grep -c "DO NOT TOUCH" → ≥ 1
      4. grep -c "When:" → ≥ 3
    Expected Result: All new sections present
    Failure Indicators: Any grep returns 0
    Evidence: .sisyphus/evidence/task-2-new-sections.txt
  ```

  **Commit**: YES
  - Message: `docs(mind): unified AGENTS.md with ADHD + schema + compression guard`
  - Files: `/home/nxyme/N-Xyme_MIND/AGENTS.md`

---

- [ ] 3. Rewrite CATALYST AGENTS.md with unified rules

  **What to do**:
  - Read current `/run/media/nxyme/NXYME_CORE/01_CODING/00_N-Xyme_CATALYST/AGENTS.md` (239 lines)
  - Rewrite using unified rules inventory from Task 1
  - PRESERVE VERBATIM: Schema checking rule, No agent duplication rule, JSON validation rule, User Preferences (Surgeon Frankenstein), Hardware Profile
  - ADOPT FROM MIND: Circuit breakers, Four Gates methodology, Delegation by default, Parallel agent firing, Memory system usage
  - ADD NEW: ADHD Operating Protocol, Compression Guard, Loop Prevention, Progressive Disclosure, Context-Activated Rules
  - KEEP ≤ 500 lines total
  - Add version field: `<!-- Version: 3.0 | Updated: 2026-03-31 -->`

  **Must NOT do**:
  - Do NOT remove existing schema checking rule
  - Do NOT remove no-duplication rule
  - Do NOT change compression hooks or behavior
  - Do NOT modify MCP server configurations
  - Do NOT change model assignments

  **Recommended Agent Profile**:
  - **Category**: `deep`
    - Reason: Complex document rewrite requiring understanding of all research sources
  - **Skills**: []
    - No special skills needed

  **Parallelization**:
  - **Can Run In Parallel**: YES
  - **Parallel Group**: Wave 1 (with Tasks 1, 2)
  - **Blocks**: Tasks 4-8
  - **Blocked By**: Task 1 (needs rules inventory)

  **References**:
  - `/run/media/nxyme/NXYME_CORE/01_CODING/00_N-Xyme_CATALYST/AGENTS.md` — Current file to preserve + extend
  - `.sisyphus/drafts/agents-md-overhaul.md` — Research synthesis
  - MIND circuit breakers: Token Budget >80%, Step Limit 10, Timeout 5min, Failure Limit 3
  - MIND Four Gates: Planning/Implementation/Review/Merge
  - MIND delegation: "DELEGATE by default"
  - Deprecated lesson: Dynamic scaling over hardcoded limits

  **Acceptance Criteria**:
  - [ ] File ≤ 500 lines (`wc -l` check)
  - [ ] Version header present
  - [ ] Schema checking rule preserved verbatim
  - [ ] No agent duplication rule preserved
  - [ ] JSON validation rule preserved
  - [ ] New "Circuit Breakers" section from MIND added
  - [ ] New "Four Gates" section from MIND added
  - [ ] New "Compression Guard" section present with "DO NOT TOUCH"
  - [ ] No references to non-existent files

  **QA Scenarios**:

  ```
  Scenario: Line count within budget
    Tool: Bash (wc -l)
    Preconditions: Rewritten AGENTS.md exists
    Steps:
      1. Run: wc -l /run/media/nxyme/NXYME_CORE/01_CODING/00_N-Xyme_CATALYST/AGENTS.md
      2. Verify output ≤ 500
    Expected Result: Line count ≤ 500
    Failure Indicators: Line count > 500
    Evidence: .sisyphus/evidence/task-3-line-count.txt

  Scenario: CATALYST-specific rules preserved
    Tool: Bash (grep)
    Preconditions: Rewritten AGENTS.md exists
    Steps:
      1. grep -c "CHECK SCHEMA" → ≥ 1
      2. grep -c "NEVER DUPLICATE" → ≥ 1
      3. grep -c "json.tool" → ≥ 1
      4. grep -c "surgeon" → ≥ 1
    Expected Result: All CATALYST-specific rules present
    Failure Indicators: Any grep returns 0
    Evidence: .sisyphus/evidence/task-3-preservation.txt

  Scenario: MIND rules adopted
    Tool: Bash (grep)
    Preconditions: Rewritten AGENTS.md exists
    Steps:
      1. grep -c "Circuit Breaker" → ≥ 1
      2. grep -c "Four Gates" → ≥ 1
      3. grep -c "DELEGATE" → ≥ 1
    Expected Result: MIND rules present in CATALYST
    Failure Indicators: Any grep returns 0
    Evidence: .sisyphus/evidence/task-3-mind-adoption.txt
  ```

  **Commit**: YES
  - Message: `docs(catalyst): unified AGENTS.md with ADHD + schema + compression guard`
  - Files: `/run/media/nxyme/NXYME_CORE/01_CODING/00_N-Xyme_CATALYST/AGENTS.md`

---

- [ ] 4. Add ADHD Operating Protocol section to both AGENTS.md files

  **What to do**:
  - Add the following rules to BOTH MIND and CATALYST AGENTS.md under "## ADHD Operating Protocol":
    - Chunked output: max 5 items per response block with clear headers
    - Step indicators: "Step X of Y" for all multi-step tasks
    - Visual progress: ✅ completion markers
    - Context anchor: "We are working on [X]" at session start and after shifts
    - State snapshot: "Before we move to Y, here's where we are: [bullets]"
    - Decision fatigue: binary choices max, sensible defaults, auto-proceed
    - Micro-celebrations: "Good, that part works"
    - Progressive disclosure: "Tell me more" hooks for deeper info
    - Interrupt model: CRITICAL/URGENT/STOP/ABORT/EMERGENCY only (preserve existing)
  - Keep each subsection ≤ 5 lines with concrete example

  **Must NOT do**:
  - Do NOT change the existing interrupt model
  - Do NOT add compression rules
  - Do NOT exceed 50 items in this section

  **Recommended Agent Profile**:
  - **Category**: `quick`
    - Reason: Adding a well-defined section to existing files
  - **Skills**: []
    - No special skills needed

  **Parallelization**:
  - **Can Run In Parallel**: YES
  - **Parallel Group**: Wave 2 (with Tasks 5, 6, 7, 8)
  - **Blocks**: Task 9
  - **Blocked By**: Tasks 2, 3 (need rewritten AGENTS.md files)

  **Acceptance Criteria**:
  - [ ] "ADHD Operating Protocol" section in MIND AGENTS.md
  - [ ] "ADHD Operating Protocol" section in CATALYST AGENTS.md
  - [ ] Each rule has concrete example
  - [ ] Interrupt model preserved (CRITICAL/URGENT/STOP/ABORT/EMERGENCY)

  **QA Scenarios**:
  ```
  Scenario: ADHD section present in both files
    Tool: Bash (grep)
    Steps:
      1. grep -c "ADHD Operating Protocol" /home/nxyme/N-Xyme_MIND/AGENTS.md → ≥ 1
      2. grep -c "ADHD Operating Protocol" /run/media/nxyme/NXYME_CORE/01_CODING/00_N-Xyme_CATALYST/AGENTS.md → ≥ 1
    Expected Result: Section present in both files
    Evidence: .sisyphus/evidence/task-4-adhd-section.txt
  ```

  **Commit**: NO (grouped with Tasks 2/3)

---

- [ ] 5. Add Schema Safety Protocol section to both AGENTS.md files

  **What to do**:
  - Add the following rules to BOTH MIND and CATALYST AGENTS.md under "## Schema Safety Protocol":
    - PRE-FLIGHT: Run lsp_diagnostics + lsp_symbols + lsp_goto_definition BEFORE writing code
    - CONTRACT-FIRST: Read interfaces/types, check usages via lsp_find_references BEFORE modifying
    - INCREMENTAL: Run lsp_diagnostics after EVERY file modification
    - BOUNDARY SCOPE: List files in scope, don't touch out-of-scope files
    - SCHEMA DRIFT: Never remove existing types, only extend. If types seem wrong, ASK
    - JSON VALIDATION: Run `python3 -m json.tool` before saving config files (from CATALYST)
    - NO DUPLICATION: opencode.json agent: (singular) = build, plan ONLY. oh-my-opencode.json agents: (plural) = ALL others (from CATALYST)
  - Include concrete commands for each rule

  **Must NOT do**:
  - Do NOT reference LSP tools that don't exist in the workspace
  - Do NOT add schema validation code (documentation only)

  **Recommended Agent Profile**:
  - **Category**: `quick`
    - Reason: Adding a well-defined section to existing files
  - **Skills**: []
    - No special skills needed

  **Parallelization**:
  - **Can Run In Parallel**: YES
  - **Parallel Group**: Wave 2 (with Tasks 4, 6, 7, 8)
  - **Blocks**: Task 9
  - **Blocked By**: Tasks 2, 3

  **Acceptance Criteria**:
  - [ ] "Schema Safety Protocol" section in MIND AGENTS.md
  - [ ] "Schema Safety Protocol" section in CATALYST AGENTS.md
  - [ ] Pre-flight check with exact lsp_diagnostics command
  - [ ] JSON validation rule from CATALYST included
  - [ ] No agent duplication rule from CATALYST included

  **QA Scenarios**:
  ```
  Scenario: Schema section with concrete commands
    Tool: Bash (grep)
    Steps:
      1. grep -c "lsp_diagnostics" /home/nxyme/N-Xyme_MIND/AGENTS.md → ≥ 2
      2. grep -c "Schema Safety" /home/nxyme/N-Xyme_MIND/AGENTS.md → ≥ 1
      3. grep -c "json.tool" /run/media/nxyme/NXYME_CORE/01_CODING/00_N-Xyme_CATALYST/AGENTS.md → ≥ 1
    Expected Result: Schema rules with concrete commands in both files
    Evidence: .sisyphus/evidence/task-5-schema.txt
  ```

  **Commit**: NO (grouped with Tasks 2/3)

---

- [ ] 6. Add Compression Guard section to both AGENTS.md files

  **What to do**:
  - Add the following section to BOTH MIND and CATALYST AGENTS.md under "## Compression Guard":
    - 🚨 ABSOLUTE RULE: DO NOT TOUCH COMPRESSION
    - History: "Compression loops wasted 12 hours" (from HANDOFF.md)
    - History: "Context limits causing user to scream 'U KEEP STOPPING'" (from HANDOFF.md)
    - Status: All compression hooks DISABLED (context-window-monitor, preemptive-compaction, anthropic-context-window-limit-recovery)
    - Rule: DO NOT re-enable, modify, or suggest compression improvements
    - Rule: Keep existing "Compress: use compress tool when research/implementation concluded" line
    - Rule: NEVER add proactive compression rules that caused past problems
    - Lesson from deprecated: "aggressive_truncation: false — Context now persists across turns (was truncating at 999 bytes)"
  - This section MUST be ≤ 15 lines

  **Must NOT do**:
  - Do NOT suggest compression improvements
  - Do NOT add compression hooks
  - Do NOT modify any DISABLED hooks

  **Recommended Agent Profile**:
  - **Category**: `quick`
    - Reason: Adding a small, well-defined section
  - **Skills**: []
    - No special skills needed

  **Parallelization**:
  - **Can Run In Parallel**: YES
  - **Parallel Group**: Wave 2 (with Tasks 4, 5, 7, 8)
  - **Blocks**: Task 9
  - **Blocked By**: Tasks 2, 3

  **Acceptance Criteria**:
  - [ ] "Compression Guard" section in MIND AGENTS.md with "DO NOT TOUCH"
  - [ ] "Compression Guard" section in CATALYST AGENTS.md with "DO NOT TOUCH"
  - [ ] Historical failure reason documented
  - [ ] All 3 DISABLED hooks listed
  - [ ] Section ≤ 15 lines

  **QA Scenarios**:
  ```
  Scenario: Compression guard with history
    Tool: Bash (grep)
    Steps:
      1. grep -c "DO NOT TOUCH" /home/nxyme/N-Xyme_MIND/AGENTS.md → ≥ 1
      2. grep -c "12 hours" /home/nxyme/N-Xyme_MIND/AGENTS.md → ≥ 1
      3. grep -c "context-window-monitor" /home/nxyme/N-Xyme_MIND/AGENTS.md → ≥ 1
      4. grep -c "DO NOT TOUCH" /run/media/nxyme/NXYME_CORE/01_CODING/00_N-Xyme_CATALYST/AGENTS.md → ≥ 1
    Expected Result: Compression guard with history in both files
    Evidence: .sisyphus/evidence/task-6-compression.txt
  ```

  **Commit**: NO (grouped with Tasks 2/3)

---

- [ ] 7. Add Loop Prevention + Circuit Breakers section to both AGENTS.md files

  **What to do**:
  - Ensure "## Circuit Breakers" section exists in BOTH files with:
    - Token Budget: Monitor context usage. If >80% capacity, compress immediately
    - Step Limit: If task exceeds 10 steps without progress, STOP and reassess
    - Timeout: Background tasks timeout after 5 minutes
    - Failure Limit: After 3 consecutive failures, STOP, revert, consult Oracle
    - Scope Creep Prevention: If finding unrelated issues, log separately
  - Add "## Loop Prevention" subsection with:
    - Progress detection: current state must differ from previous state
    - Tool call diversity: ensure variety in tool calls (don't repeat same tool with same params)
    - OnLoop behavior: if stuck, STOP and escalate (never infinite retry)
    - Lesson from deprecated: "Metis/Momus were omni — caused loops" → use fast/cheap models for evaluation
  - Ensure MIND's existing circuit breakers are PRESERVED VERBATIM

  **Must NOT do**:
  - Do NOT change existing circuit breaker thresholds
  - Do NOT add infinite retry patterns

  **Recommended Agent Profile**:
  - **Category**: `quick`
    - Reason: Adding/verifying a well-defined section
  - **Skills**: []
    - No special skills needed

  **Parallelization**:
  - **Can Run In Parallel**: YES
  - **Parallel Group**: Wave 2 (with Tasks 4, 5, 6, 8)
  - **Blocks**: Task 9
  - **Blocked By**: Tasks 2, 3

  **Acceptance Criteria**:
  - [ ] Circuit Breakers section in MIND AGENTS.md (preserved)
  - [ ] Circuit Breakers section in CATALYST AGENTS.md (adopted from MIND)
  - [ ] Loop Prevention subsection in both files
  - [ ] Omni model lesson included

  **QA Scenarios**:
  ```
  Scenario: Circuit breakers + loop prevention
    Tool: Bash (grep)
    Steps:
      1. grep -c "Circuit Breaker" /home/nxyme/N-Xyme_MIND/AGENTS.md → ≥ 1
      2. grep -c "80%" /home/nxyme/N-Xyme_MIND/AGENTS.md → ≥ 1
      3. grep -c "Loop Prevention" /home/nxyme/N-Xyme_MIND/AGENTS.md → ≥ 1
      4. grep -c "omni" /home/nxyme/N-Xyme_MIND/AGENTS.md → ≥ 1
    Expected Result: Both sections present with omni lesson
    Evidence: .sisyphus/evidence/task-7-loop-prevention.txt
  ```

  **Commit**: NO (grouped with Tasks 2/3)

---

- [ ] 8. Add Deprecated Lessons section + Context-Activated Rules to both AGENTS.md files

  **What to do**:
  - Add "## Lessons from Previous Iterations" section to BOTH files with:
    - COMPRESSION/TRUNCATION: Never truncate context. "aggressive_truncation: false" was the fix.
    - OMNI MODELS: Evaluation agents (Metis, Momus) need fast/cheap models. Omni caused loops.
    - SCOPE CREEP: 6/8 previous iterations failed from feature bloat. Keep it simple.
    - DOCKER: Failed across multiple iterations. Native Linux first.
    - AGENT DUPLICATION: opencode.json vs oh-my-opencode.json — one source of truth.
    - HARDCODED LIMITS: Use dynamic/configurable, not hardcoded.
    - TOO MANY MCP SERVERS: Only enable what you actually use.
  - Add "## Context-Activated Rules" section with When: blocks:
    - `### When: debugging` — Enable verbose logging, don't modify files
    - `### When: adding-feature` — Check existing patterns first, add tests
    - `### When: refactoring` — Preserve all public interfaces, add migration notes
    - `### When: config-change` — CHECK SCHEMA FIRST (from CATALYST), validate JSON
    - `### When: performance` — Profile before optimizing, document tradeoffs
  - Keep lessons section ≤ 20 lines, When: blocks ≤ 5 lines each

  **Must NOT do**:
  - Do NOT add more than 5 When: blocks
  - Do NOT make lessons too long (this is reference, not documentation)

  **Recommended Agent Profile**:
  - **Category**: `quick`
    - Reason: Adding well-defined sections from existing research
  - **Skills**: []
    - No special skills needed

  **Parallelization**:
  - **Can Run In Parallel**: YES
  - **Parallel Group**: Wave 2 (with Tasks 4, 5, 6, 7)
  - **Blocks**: Task 9
  - **Blocked By**: Tasks 2, 3

  **Acceptance Criteria**:
  - [ ] "Lessons from Previous Iterations" section in both files
  - [ ] ≥ 5 lessons documented
  - [ ] "Context-Activated Rules" section with ≥ 3 When: blocks
  - [ ] Each When: block ≤ 5 lines

  **QA Scenarios**:
  ```
  Scenario: Lessons + context-activated rules
    Tool: Bash (grep)
    Steps:
      1. grep -c "Lessons from" /home/nxyme/N-Xyme_MIND/AGENTS.md → ≥ 1
      2. grep -c "When:" /home/nxyme/N-Xyme_MIND/AGENTS.md → ≥ 3
      3. grep -c "When:" /run/media/nxyme/NXYME_CORE/01_CODING/00_N-Xyme_CATALYST/AGENTS.md → ≥ 3
    Expected Result: Lessons and When: blocks in both files
    Evidence: .sisyphus/evidence/task-8-lessons.txt
  ```

  **Commit**: NO (grouped with Tasks 2/3)

---

- [ ] 9. Optimize parallel agent concurrency for your hardware

  **What to do**:
  - Add `background_task` section to `config/opencode.json` with hardware-optimized concurrency
  - Your hardware: Ryzen 7800X3D (8C/16T), RTX 3080 Ti (12GB), 32GB DDR5
  - Scale 4-5x from deprecated MIND defaults (which were conservative)

  **Proposed config addition:**
  ```json
  "background_task": {
    "defaultConcurrency": 64,
    "staleTimeoutMs": 300000,
    "providerConcurrency": {
      "opencode": 80
    },
    "modelConcurrency": {
      "opencode/mimo-v2-pro-free": 32,
      "opencode/minimax-m2.5-free": 64,
      "opencode/gpt-5-nano": 128,
      "opencode/kimi-k2.5-free": 32
    }
  }
  ```

  **Rationale:**
  - Deprecated MIND had: default=16, provider=20, mimo=8, minimax=16, nano=32
  - 4-5x those: default=64-80, provider=80-100, mimo=32-40, minimax=64-80, nano=128-160
  - Rounded to clean numbers that won't cause API rate limit issues
  - staleTimeoutMs increased from 180s to 300s for slower free-tier APIs

  **Must NOT do**:
  - Do NOT exceed API rate limits of free models (mimo: 80 req/min, minimax: 120 req/min)
  - Do NOT set concurrency higher than CPU threads (16) for CPU-bound tasks
  - Do NOT touch compression settings

  **Recommended Agent Profile**:
  - **Category**: `quick`
    - Reason: Adding JSON config section
  - **Skills**: []
    - No special skills needed

  **Parallelization**:
  - **Can Run In Parallel**: YES
  - **Parallel Group**: Wave 2 (with Tasks 4-8)
  - **Blocks**: Task 10
  - **Blocked By**: None

  **Acceptance Criteria**:
  - [ ] `background_task` section added to config/opencode.json
  - [ ] defaultConcurrency ≥ 64
  - [ ] JSON validates with python3 -m json.tool

  **QA Scenarios**:
  ```
  Scenario: Concurrency config present and valid
    Tool: Bash (grep, python3)
    Steps:
      1. grep -c "background_task" config/opencode.json → ≥ 1
      2. grep -c "defaultConcurrency" config/opencode.json → ≥ 1
      3. python3 -m json.tool config/opencode.json > /dev/null → exit 0
    Expected Result: Config present and valid JSON
    Evidence: .sisyphus/evidence/task-9-parallelism.txt
  ```

  **Commit**: YES
  - Message: `perf: optimize parallel agent concurrency for 7800X3D hardware`
  - Files: `config/opencode.json`

---

- [ ] 10. Create global cross-project rules file

  **What to do**:
  - Create `.sisyphus/rules/global-rules.md` with rules that apply across BOTH MIND and CATALYST
  - Include:
    - Compression Guard (DO NOT TOUCH — with history)
    - Schema Safety Protocol (pre-flight, contract-first, incremental)
    - ADHD Operating Protocol (chunked, steps, anchors)
    - Circuit Breakers (token, step, timeout, failure)
    - Loop Prevention (progress detection, tool diversity, omni lesson)
    - Delegation Rules (delegate by default, parallel agents)
    - Verification (nothing done without proof)
    - Lessons from deprecated (7 mistakes, 8 successes)
  - Keep ≤ 300 lines
  - Reference both AGENTS.md files as source of truth for project-specific rules

  **Must NOT do**:
  - Do NOT duplicate rules that are project-specific
  - Do NOT reference files that don't exist
  - Do NOT add compression changes

  **Recommended Agent Profile**:
  - **Category**: `quick`
    - Reason: Creating a concise cross-project rules file
  - **Skills**: []
    - No special skills needed

  **Parallelization**:
  - **Can Run In Parallel**: NO
  - **Parallel Group**: Sequential (after Wave 2)
  - **Blocks**: Task 10
  - **Blocked By**: Tasks 4-8 (needs all sections finalized)

  **Acceptance Criteria**:
  - [ ] File created at `.sisyphus/rules/global-rules.md`
  - [ ] ≤ 300 lines
  - [ ] Compression guard present
  - [ ] Schema safety present
  - [ ] ADHD protocol present
  - [ ] References both AGENTS.md files

  **QA Scenarios**:
  ```
  Scenario: Global rules file exists and complete
    Tool: Bash (wc -l, grep)
    Steps:
      1. wc -l .sisyphus/rules/global-rules.md → ≤ 300
      2. grep -c "DO NOT TOUCH" → ≥ 1
      3. grep -c "Schema Safety" → ≥ 1
      4. grep -c "ADHD" → ≥ 1
    Expected Result: File exists with all required sections
    Evidence: .sisyphus/evidence/task-9-global-rules.txt
  ```

  **Commit**: YES
  - Message: `docs: add global cross-project rules`
  - Files: `.sisyphus/rules/global-rules.md`

---

- [ ] 10. Final verification and cleanup

  **What to do**:
  - Run all QA scenarios from all tasks
  - Verify line counts: both AGENTS.md ≤ 500, global-rules.md ≤ 300
  - Verify all existing rules preserved (grep for key sections)
  - Verify compression guard present with "DO NOT TOUCH"
  - Verify schema safety present with lsp_diagnostics commands
  - Verify ADHD protocols present with chunked/step/anchor rules
  - Verify lessons from deprecated documented
  - Verify no broken references (grep for file paths, check they exist)
  - Clean up any formatting issues

  **Must NOT do**:
  - Do NOT change any rules during verification
  - Do NOT touch compression

  **Recommended Agent Profile**:
  - **Category**: `quick`
    - Reason: Verification only, no content changes
  - **Skills**: []
    - No special skills needed

  **Parallelization**:
  - **Can Run In Parallel**: NO
  - **Parallel Group**: Sequential (after Task 9)
  - **Blocks**: Task 11
  - **Blocked By**: Task 9

  **Acceptance Criteria**:
  - [ ] All QA scenarios pass
  - [ ] Line counts within budget
  - [ ] All existing rules preserved
  - [ ] All new sections present
  - [ ] No broken references

  **Commit**: NO (verification only)

---

- [ ] 11. Create quality gate scripts

  **What to do**:
  - Create `bin/quality-gates/` directory with executable shell scripts
  - Each script returns exit 0 (pass) or exit 1 (fail)
  - Gates are DETERMINISTIC — agents can't game them

  **Scripts to create:**
  - `gate-1-typecheck.sh` — runs `tsc --noEmit` (TypeScript strict check)
  - `gate-2-lint.sh` — runs ESLint or Biome
  - `gate-3-format.sh` — runs Prettier check (flag only, not fix)
  - `gate-4-test.sh` — runs Vitest
  - `gate-all.sh` — chains gates 1-4, stops on first failure

  **Rationale (from Oracle):**
  - "Gates as shell scripts, not agents — deterministic, can't be gamed"
  - "An agent reviewing code can be convinced by another agent. A type checker cannot."
  - Mandatory gates: 1 (typecheck), 2 (lint), 4 (test). Optional: 3 (format).

  **Must NOT do**:
  - Do NOT create agent-based quality checks (use shell scripts)
  - Do NOT allow gates to be skipped in prompts

  **Recommended Agent Profile**:
  - **Category**: `quick`
    - Reason: Writing shell scripts for tool invocation
  - **Skills**: []
    - No special skills needed

  **Parallelization**:
  - **Can Run In Parallel**: YES
  - **Parallel Group**: Wave 2 (with Tasks 4-9)
  - **Blocks**: Task 13
  - **Blocked By**: None

  **Acceptance Criteria**:
  - [ ] `bin/quality-gates/` directory exists
  - [ ] gate-1-typecheck.sh exists and is executable
  - [ ] gate-2-lint.sh exists and is executable
  - [ ] gate-4-test.sh exists and is executable
  - [ ] Each script returns exit 0 on success, exit 1 on failure

  **Commit**: YES
  - Message: `infra: add quality gate scripts for automated code verification`
  - Files: `bin/quality-gates/*.sh`

---

- [ ] 12. Create masterprompt template

  **What to do**:
  - Create `.sisyphus/masterprompt.md` with the architect-first delegation template
  - Include: spec-driven workflow, quality gates in prompt, agent assignment rules, review separation

  **Masterprompt structure:**
  ```
  ## Task Decomposition Protocol
  1. Break into independent work units (files/modules)
  2. Assign by agent profile:
     - UI/visual → category: visual-engineering (kimi)
     - Complex logic → category: deep (mimo-medium)
     - Quick fixes → category: quick (gpt-5-nano)
     - Tests → agent: sisyphus-junior (minimax)
     - Review → agent: oracle (mimo-high)
  3. Fire all independent units in parallel
  4. After each unit → run quality gates
  5. After ALL units → oracle review pass
  6. Merge only after gates + review pass

  ## Quality Gates (Mandatory)
  - Gate 1: tsc --noEmit (type safety)
  - Gate 2: eslint (code quality)
  - Gate 4: vitest (tests)

  ## Review Separation (Adversarial)
  - Implementor: hephaestus/sisyphus-junior
  - Reviewer: oracle (DIFFERENT agent, DIFFERENT perspective)
  - Red-team: momus (AFTER oracle approves)
  - Merge: only after BOTH reviewers pass

  ## Architect's Job (NOT Code)
  - Write specs with interfaces + acceptance criteria
  - Select agent profiles per task
  - Review oracle's architecture summary
  - Approve/reject based on gates + review
  ```

  **Must NOT do**:
  - Do NOT include implementation instructions (architect doesn't code)
  - Do NOT reference agents that don't exist in config

  **Recommended Agent Profile**:
  - **Category**: `quick`
    - Reason: Writing a template document
  - **Skills**: []
    - No special skills needed

  **Parallelization**:
  - **Can Run In Parallel**: YES
  - **Parallel Group**: Wave 2 (with Tasks 4-9, 11)
  - **Blocks**: Task 13
  - **Blocked By**: None

  **Acceptance Criteria**:
  - [ ] `.sisyphus/masterprompt.md` exists
  - [ ] Contains task decomposition protocol
  - [ ] Contains quality gates section
  - [ ] Contains review separation rules
  - [ ] Contains architect role definition

  **Commit**: YES
  - Message: `docs: add masterprompt template for architect-first delegation`
  - Files: `.sisyphus/masterprompt.md`

---

- [ ] 13. Add architect-first rules to AGENTS.md files

  **What to do**:
  - Add "## Architect-First Code Quality" section to BOTH AGENTS.md files
  - Include: quality gate requirements, model-for-code rule, review separation

  **Rules to add:**
  - NEVER use `quick` (gpt-5-nano) for implementation tasks — only for config/typo fixes
  - Implementation tasks: `deep` (mimo-medium) or `unspecified-high` (mimo-high) MINIMUM
  - UI tasks: `visual-engineering` (kimi-high) MINIMUM
  - Quality gates MANDATORY before declaring done: typecheck + lint + test
  - Review separation: oracle reviews AFTER implementation, NOT the same agent
  - Architect writes specs, NOT code

  **Must NOT do**:
  - Do NOT change existing agent model assignments
  - Do NOT add new agents

  **Recommended Agent Profile**:
  - **Category**: `quick`
    - Reason: Adding section to existing documents
  - **Skills**: []
    - No special skills needed

  **Parallelization**:
  - **Can Run In Parallel**: NO
  - **Parallel Group**: Sequential (after Tasks 4-10)
  - **Blocks**: Task 14
  - **Blocked By**: Tasks 4-10

  **Acceptance Criteria**:
  - [ ] "Architect-First Code Quality" section in MIND AGENTS.md
  - [ ] "Architect-First Code Quality" section in CATALYST AGENTS.md
  - [ ] Rule: "NEVER use quick for implementation" present
  - [ ] Rule: "Quality gates mandatory" present
  - [ ] Rule: "Review separation" present

  **Commit**: YES
  - Message: `docs: add architect-first code quality rules`
  - Files: Both AGENTS.md files

---

- [ ] 14. Delete draft file

  **What to do**:
  - Delete `.sisyphus/drafts/agents-md-overhaul.md`
  - The draft served its purpose — all research is now in the plan and AGENTS.md files

  **Must NOT do**:
  - Do NOT delete the plan file

  **Recommended Agent Profile**:
  - **Category**: `quick`
    - Reason: Simple file deletion
  - **Skills**: []
    - No special skills needed

  **Parallelization**:
  - **Can Run In Parallel**: NO
  - **Parallel Group**: Sequential (after Task 13)
  - **Blocks**: None
  - **Blocked By**: Task 13

  **Acceptance Criteria**:
  - [ ] Draft file deleted

  **Commit**: NO (cleanup only)

---

## Final Verification Wave

- [ ] F1. **Line count check** — Bash
  - `wc -l /home/nxyme/N-Xyme_MIND/AGENTS.md` → ≤ 500
  - `wc -l /run/media/nxyme/NXYME_CORE/01_CODING/00_N-Xyme_CATALYST/AGENTS.md` → ≤ 500

- [ ] F2. **Rule preservation check** — Bash
  - `grep -c "Never Do" /home/nxyme/N-Xyme_MIND/AGENTS.md` → ≥ 1
  - `grep -c "VPN" /home/nxyme/N-Xyme_MIND/AGENTS.md` → ≥ 1
  - `grep -c "Circuit Breaker" /home/nxyme/N-Xyme_MIND/AGENTS.md` → ≥ 1
  - `grep -c "Schema" /run/media/nxyme/NXYME_CORE/01_CODING/00_N-Xyme_CATALYST/AGENTS.md` → ≥ 1

- [ ] F3. **Compression guard check** — Bash
  - `grep -c "DO NOT TOUCH" /home/nxyme/N-Xyme_MIND/AGENTS.md` → ≥ 1
  - `grep -c "DO NOT TOUCH" /run/media/nxyme/NXYME_CORE/01_CODING/00_N-Xyme_CATALYST/AGENTS.md` → ≥ 1

- [ ] F4. **New sections check** — Bash
  - `grep -c "ADHD" /home/nxyme/N-Xyme_MIND/AGENTS.md` → ≥ 2
  - `grep -c "Schema Safety" /home/nxyme/N-Xyme_MIND/AGENTS.md` → ≥ 1
  - `grep -c "When:" /home/nxyme/N-Xyme_MIND/AGENTS.md` → ≥ 3

---

## Commit Strategy

- **1**: `docs(mind): unified AGENTS.md with ADHD + schema + compression guard` — AGENTS.md
- **2**: `docs(catalyst): unified AGENTS.md with ADHD + schema + compression guard` — AGENTS.md
- **3**: `docs: add global cross-project rules` — .sisyphus/rules/global-rules.md

---

## Success Criteria

### Verification Commands
```bash
wc -l /home/nxyme/N-Xyme_MIND/AGENTS.md  # Expected: ≤ 500
wc -l /run/media/nxyme/NXYME_CORE/01_CODING/00_N-Xyme_CATALYST/AGENTS.md  # Expected: ≤ 500
grep -c "DO NOT TOUCH" /home/nxyme/N-Xyme_MIND/AGENTS.md  # Expected: ≥ 1
grep -c "Schema Safety" /home/nxyme/N-Xyme_MIND/AGENTS.md  # Expected: ≥ 1
grep -c "ADHD" /home/nxyme/N-Xyme_MIND/AGENTS.md  # Expected: ≥ 2
```

### Final Checklist
- [ ] MIND AGENTS.md ≤ 500 lines
- [ ] CATALYST AGENTS.md ≤ 500 lines
- [ ] All existing rules preserved
- [ ] Compression explicitly guarded
- [ ] Schema safety protocol added
- [ ] ADHD protocols systematized
- [ ] Deprecated lessons documented
- [ ] No broken references
