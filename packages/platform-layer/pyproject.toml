[project]
name = "n-xyme-platform-layer"
version = "0.1.0"
description = "CLI tools, dashboard, TUI, and operational scripts"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
nxyme = "packages.platform_layer.launcher:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["packages.platform_layer*"]
