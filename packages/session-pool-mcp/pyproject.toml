[project]
name = "n-xyme-session-pool"
version = "0.1.0"
description = "Pre-warmed session pool for OMO agents"
requires-python = ">=3.12"
dependencies = [
    "fastmcp",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"